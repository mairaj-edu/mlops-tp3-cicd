name: MLOps CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run all tests
        env:
          PYTHONPATH: .
        run: pytest tests/ -v --cov=src --cov-fail-under=60

  # Job d'enregistrement désactivé sur GitHub Actions
  # (MLflow local n'est pas accessible depuis les runners)
  # À réactiver plus tard avec un serveur MLflow distant si besoin.
  # register-model:
  #   needs: test
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #     - run: pip install -r requirements.txt
  #     - run: python train_and_register.py

